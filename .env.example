# Copy to .env — Supabase → Project Settings → API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Where the app is served from. Used to build email asset URLs and CTA links.
# Not the Wix marketing site (joinalyne.com) — that has no /email or /app path.
APP_URL=https://alyne-git-main-alyne-s-projects.vercel.app
